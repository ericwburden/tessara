http:
  routers:
    dashboard-active:
      entryPoints: [module]
      rule: PathPrefix(`/`)
      service: dashboard-active
  services:
    dashboard-active:
      loadBalancer:
        servers:
          - url: http://dashboards-candidate:8091
