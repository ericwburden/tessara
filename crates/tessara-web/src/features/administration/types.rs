//! Administration type compatibility surface.
//!
//! The concrete type declarations remain in `models.rs`. This module keeps the
//! planned crate layout while preserving current public behavior.

pub(crate) use super::models::*;

