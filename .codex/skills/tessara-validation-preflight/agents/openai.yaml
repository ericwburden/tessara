interface:
  display_name: "Tessara Validation Preflight"
  short_description: "Audit final gates and freeze a candidate"
  default_prompt: "Use $tessara-validation-preflight to audit passing readiness, rehearsal, and any required convergence records, then freeze a source-exact candidate."
